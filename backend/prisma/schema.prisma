generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model users {
  id                  Int       @id @default(autoincrement())
  emri                String    @db.VarChar(100)
  mbiemri             String    @db.VarChar(100)
  email               String    @unique(map: "email") @db.VarChar(150)
  password_hash       String    @db.VarChar(255)
  phone_number        String?   @db.VarChar(30)
  email_confirmed     Boolean?  @default(false)
  lockout_enabled     Boolean?  @default(false)
  access_failed_count Int?      @default(0)
  lockout_end         DateTime? @db.DateTime(0)
  statusi             String?   @default("aktiv") @db.VarChar(50)
  notifications_enabled Boolean @default(true)
  created_at          DateTime? @default(now()) @db.DateTime(0)
  updated_at          DateTime? @default(now()) @db.DateTime(0)

  userroles           userroles[]
  notifications       notifications[]
  login_history       loginhistory[]
  social_accounts     socialmediaaccounts[]
  campaigns           campaigns[]
  leads               leads[]
}

model roles {
  id              Int       @id @default(autoincrement())
  emertimi        String    @db.VarChar(100)
  pershkrimi      String?   @db.Text
  normalized_name String    @unique(map: "normalized_name") @db.VarChar(100)
  created_at      DateTime? @default(now()) @db.DateTime(0)
  updated_at      DateTime? @default(now()) @db.DateTime(0)

  userroles       userroles[]
}

model userroles {
  id         Int       @id @default(autoincrement())
  user_id    Int
  role_id    Int
  created_at DateTime? @default(now()) @db.DateTime(0)
  updated_at DateTime? @default(now()) @db.DateTime(0)

  @@unique([user_id, role_id], map: "user_id")
  @@index([role_id], map: "fk_userroles_role")

  user users @relation(fields: [user_id], references: [id])
  role roles @relation(fields: [role_id], references: [id])
}

model refreshtokens {
  id         Int       @id @default(autoincrement())
  user_id    Int
  token      String    @db.Text
  expires    DateTime  @db.DateTime(0)
  revoked    DateTime? @db.DateTime(0)
  created_at DateTime? @default(now()) @db.DateTime(0)
  updated_at DateTime? @default(now()) @db.DateTime(0)

  @@index([user_id], map: "fk_refreshtokens_user")
}

model usertokens {
  id             Int       @id @default(autoincrement())
  user_id        Int
  login_provider String    @db.VarChar(100)
  token_name     String    @db.VarChar(100)
  token_value    String    @db.Text
  created_at     DateTime? @default(now()) @db.DateTime(0)
  updated_at     DateTime? @default(now()) @db.DateTime(0)

  @@index([user_id], map: "fk_usertokens_user")
}

model userclaims {
  id          Int       @id @default(autoincrement())
  user_id     Int
  claim_type  String    @db.VarChar(100)
  claim_value String?   @db.Text
  created_at  DateTime? @default(now()) @db.DateTime(0)
  updated_at  DateTime? @default(now()) @db.DateTime(0)

  @@index([user_id], map: "fk_userclaims_user")
}

model notifications {
  id          Int       @id @default(autoincrement())
  user_id     Int
  message     String?   @db.Text
  read_status Boolean?  @default(false)
  created_at  DateTime? @default(now()) @db.DateTime(0)
  updated_at  DateTime? @default(now()) @db.DateTime(0)

  user        users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_notifications_user")

  @@index([user_id], map: "fk_notifications_user")
}

model loginhistory {
  id         Int       @id @default(autoincrement())
  user_id    Int
  ip_address String?   @db.VarChar(45)
  device     String?   @db.VarChar(255)
  statusi    String?   @default("success") @db.VarChar(50)
  created_at DateTime? @default(now()) @db.DateTime(0)

  user       users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_loginhistory_user")
  @@index([user_id], map: "fk_loginhistory_user")
}

model channels {
  id               Int                  @id @default(autoincrement())
  emertimi         String               @db.VarChar(100)
  lloji            String               @db.VarChar(100)
  pershkrimi       String?              @db.Text
  url              String?              @db.VarChar(255)
  statusi          String?              @default("aktiv") @db.VarChar(50)
  created_at       DateTime?            @default(now()) @db.DateTime(0)
  updated_at       DateTime?            @default(now()) @db.DateTime(0)
  
  social_account   socialmediaaccounts?
  campaignchannels campaignchannels[]
  analytics analytics[]
}

model socialmediaaccounts {
  id         Int       @id @default(autoincrement())
  user_id    Int
  platforma  String    @db.VarChar(100)
  username   String    @db.VarChar(100)
  followers  Int?      @default(0)
  statusi    String?   @default("aktiv") @db.VarChar(50)
  created_at DateTime? @default(now()) @db.DateTime(0)
  updated_at DateTime? @default(now()) @db.DateTime(0)

  channel_id Int       @unique
  channel    channels  @relation(fields: [channel_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_socialmediaaccounts_channel")
  
  user       users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_socialmediaaccounts_user")
  @@index([user_id], map: "fk_socialmediaaccounts_user")
}

model campaigns {
  id               Int       @id @default(autoincrement())
  emertimi         String    @db.VarChar(150)
  pershkrimi       String?   @db.Text
  buxheti          Decimal?  @default(0.00) @db.Decimal(12, 2)
  data_fillimit    DateTime? @db.Date
  data_perfundimit DateTime? @db.Date
  statusi          String?   @default("draft") @db.VarChar(50)
  objektivi        String?   @db.VarChar(255)
  user_id          Int
  created_at       DateTime? @default(now()) @db.DateTime(0)
  updated_at       DateTime? @default(now()) @db.DateTime(0)


  user              users               @relation(fields: [user_id], references: [id])
  contents          contents[]
  analytics         analytics[]
  budgets           budgets[]
  campaignaudiences campaignaudiences[]
  campaignchannels  campaignchannels[]
  leads             leads[]
  abtests           abtest[]
  reports           reports[]
  scheduling        scheduling[]
  dailysummary      dailysummary[]

  @@index([user_id], map: "fk_campaigns_user")
}

model contents {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  titulli     String    @db.VarChar(200)
  lloji       String?   @db.VarChar(100)
  permbajtja  String?   @db.Text
  media_url   String?   @db.VarChar(255)
  statusi     String?   @default("draft") @db.VarChar(50)
  created_at  DateTime? @default(now()) @db.DateTime(0)
  updated_at  DateTime? @default(now()) @db.DateTime(0)
}

model analytics {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  channel_id  Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  channel     channels  @relation(fields: [channel_id], references: [id])
  klikime     Int?      @default(0)
  shikime     Int?      @default(0)
  konvertime  Int?      @default(0)
}

model campaignchannels {
  id              Int       @id @default(autoincrement())
  campaign_id     Int
  channel_id      Int
  buxheti_alokuar Decimal?  @default(0.00) @db.Decimal(12, 2)
  campaign        campaigns @relation(fields: [campaign_id], references: [id])
  channel         channels  @relation(fields: [channel_id], references: [id])

  @@unique([campaign_id, channel_id])
}

model budgets {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  shuma_totale Decimal? @default(0.00) @db.Decimal(12, 2)
}

model leads {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  user_id     Int?     
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  user        users?    @relation(fields: [user_id], references: [id]) // Kjo
  email       String    @unique @db.VarChar(150)
}

model abtest {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  variant_name String?
}

model reports {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  file_path   String?
}

model scheduling {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
}

model dailysummary {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  date        DateTime  @db.Date
}

model audiences {
  id                Int                 @id @default(autoincrement())
  emertimi          String              @db.VarChar(150)
  pershkrimi        String?             @db.Text
  campaignaudiences campaignaudiences[]
}

model campaignaudiences {
  id          Int       @id @default(autoincrement())
  campaign_id Int
  audience_id Int
  campaign    campaigns @relation(fields: [campaign_id], references: [id])
  audience    audiences @relation(fields: [audience_id], references: [id])

  @@unique([campaign_id, audience_id])
} 